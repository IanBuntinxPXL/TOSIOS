pipeline{
    agent any
    stages{
        stage('Zip file'){
            steps{
              sh 'echo "Creating Archive..."'
              // sh 'zip -r -q zipFile . Releases.zip'
              // archiveArtifacts 'Releases.zip'
            }
        }
        stage('Update code'){
            steps{
                sh 'echo "test"'
            }
        }
    }
}
